name: GitGecko deep black-box benchmark v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  evidence-complete:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The full deep-benchmark matrix completed in run 30170895330."
