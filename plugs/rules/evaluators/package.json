{
  "name": "@gitgecko/plug-rules-evaluators",
  "version": "0.0.1",
  "private": true,
  "description": "Rule evaluators for the rules owner (04 §8). Three kinds: structural (ast-grep napi, P-codeintel-11), lexical (regex), llm (NL path-instructions, CR-§8). Deterministic-first evaluation via evaluateRules. The differentiator neither competitor has.",
  "type": "module",
  "exports": {
    ".": "./dist/plug.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rimraf dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@ast-grep/napi": "0.44.0",
    "@gitgecko/core": "workspace:*",
    "@gitgecko/rules": "workspace:*",
    "@gitgecko/socket": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.0.0",
    "rimraf": "6.0.0",
    "tsx": "4.19.0",
    "typescript": "5.6.3"
  },
  "main": "./dist/plug.js",
  "types": "./dist/plug.d.ts"
}
