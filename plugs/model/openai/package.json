{
  "name": "@gitgecko/plug-model-openai",
  "version": "0.0.1",
  "private": true,
  "description": "OpenAI model plug for the canonical model socket. BYOK or compatible endpoint; contributes typed generation and real streaming.",
  "type": "module",
  "exports": {
    ".": "./dist/plug.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rimraf dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@gitgecko/core": "workspace:*",
    "@gitgecko/model-client": "workspace:*",
    "@gitgecko/socket": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.0.0",
    "rimraf": "6.0.0",
    "tsx": "4.19.0",
    "typescript": "5.6.3"
  },
  "main": "./dist/plug.js",
  "types": "./dist/plug.d.ts"
}
