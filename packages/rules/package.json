{
  "name": "@gitgecko/rules",
  "version": "0.0.0",
  "private": true,
  "description": "gitgecko rules owner — the deterministic-rules wedge (04 §8). Three rule kinds: ast-grep structural (P-codeintel-11), regex/lexical, NL path-instructions (CR-§8). Deterministic-first evaluation, source-tagged findings. The differentiator neither CodeRabbit nor Greptile has.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./finding": "./dist/finding.js"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx src/**/*.test.ts"
  },
  "dependencies": {
    "@gitgecko/core": "workspace:*",
    "@gitgecko/socket": "workspace:*",
    "minimatch": "9.0.9",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "tsx": "4.19.0",
    "@types/node": "22.0.0",
    "rimraf": "6.0.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts"
}
