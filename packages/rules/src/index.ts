/**
 * @gitgecko/rules — the deterministic-rules wedge owner (04 §8).
 *
 * Three rule kinds (structural/lexical/llm), deterministic-first evaluation,
 * source-tagged findings. The differentiator neither CodeRabbit (CR-§8) nor
 * Greptile (GP-§10 wp2) has — both are NL-only.
 */
export * from "./finding.js";
export * from "./path-match.js";
