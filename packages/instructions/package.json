{
  "name": "@gitgecko/instructions",
  "version": "0.0.0",
  "private": true,
  "description": "gitgecko's review brain — persona, output-format blueprints, severity-tagged finding renderer, anti-noise guardrails, and the proprietary rules corpus that encodes how to find real defects by reasoning about consequences. Consumed by every agent backend (native, local, cloud). Every finding cites a stable, glossary-resolvable ID.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./browser": "./dist/browser.js"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx src/**/*.test.ts"
  },
  "dependencies": {
    "@gitgecko/core": "workspace:*",
    "@gitgecko/review": "workspace:*",
    "@gitgecko/rules": "workspace:*",
    "yaml": "2.9.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.0.0",
    "rimraf": "6.0.0",
    "tsx": "4.19.0",
    "typescript": "5.6.3"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts"
}
