{
  "name": "@gitgecko/code-intel",
  "version": "0.0.0",
  "private": true,
  "description": "gitgecko code-intelligence owner — the code-intel owner from 02-architecture-overview §2. Implements the ParserSocket + CodeIntelSocket. This package is the owner runtime; the actual engines (tree-sitter-parse, graph-build, etc.) are plugs under plugs/code-intel/.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./tags": "./dist/tags.js"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx src/**/*.test.ts"
  },
  "dependencies": {
    "@gitgecko/core": "workspace:*",
    "@gitgecko/socket": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "tsx": "4.19.0",
    "@types/node": "22.0.0",
    "rimraf": "6.0.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts"
}
